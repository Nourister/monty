#include "monty.h"

/*
 * nop - Does nothing.
 * @stack: Double pointer to the stack.
 * @line_number: The line number where the 'nop' opcode is.
 * Return: 0(success)
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
